{
  "name": "root",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.0.0"
  },
  "scripts": {
    "publish": "lerna publish",
    "create-packs": "lerna create package-name -y",
    "build": "learn run build",
    "cb-build": "lerna run build:prod --scope=components-base",
    "cb-i": "lerna run i --scope=components-base",
    "const-build": "lerna run build:prod --scope=constants",
    "const-i": "lerna run i --scope=constants"
  },
  "repository": {
		"url": "https://github.com/MinhLeeBtaskee/bbepacks",
		"type": "git"
	},
  "engines" : { 
    "npm" : ">=8.0.0"
  }
}