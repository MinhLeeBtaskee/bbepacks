'use strict';

const componentsBase = require('..');
const assert = require('assert').strict;

assert.strictEqual(componentsBase(), 'Hello from componentsBase');
console.info('componentsBase tests passed');
